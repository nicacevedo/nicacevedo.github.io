<div class="col">
  <div class="card h-100 hoverable">
    {% if project.img %}
      <a
        href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}"
        aria-label="View research project: {{ project.title }}"
      >
        {%
          include figure.liquid
          loading="eager"
          path=project.img
          sizes = "250px"
          alt=project.title
          class="card-img-top"
        %}
      </a>
    {% endif %}
    <div class="card-body">
      <h2 class="card-title">
        <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">{{ project.title }}</a>
      </h2>
      <p class="card-text">{{ project.description }}</p>
      {% if project.status or project.period %}
        <p class="card-text text-muted mb-0">
          <small>
            {% if project.status %}<strong>{{ project.status }}</strong>{% endif %}
            {% if project.status and project.period %} · {% endif %}
            {{ project.period }}
          </small>
        </p>
      {% endif %}
      {% if project.github %}
        <p class="card-text mt-2 mb-0">
          <small>
            <i class="fa-brands fa-github" aria-hidden="true"></i>
            <a href="{{ project.github }}" target="_blank" rel="noopener">Code / project repository<span class="sr-only"> (opens in a new tab)</span></a>
          </small>
        </p>
      {% endif %}
    </div>
  </div>
</div>
